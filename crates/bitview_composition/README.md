# bitview_composition

The official typed plugin composition and compute schedule used by Bitview.

Most users should use [`bitview`](https://crates.io/crates/bitview), which
re-exports this composition and runs it through `bitview_runtime`.
